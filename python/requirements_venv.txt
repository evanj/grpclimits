# installing the binary wheel of grpcio requires an up to date version of pip
# the version shipped with Python 3.7 in Debian is pretty ancient
# also use a recent version of setuptools just to be safe
pip==21.0.1
setuptools==56.0.0
