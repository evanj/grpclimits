# real runtime dependencies for the Python code: installed in Docker image
grpcio==1.47.0  # 1.48.0 is broken
protobuf==4.21.5
# necessary for Python < 3.8 (Debian 10 is Python 3.7)
typing-extensions==4.3.0
